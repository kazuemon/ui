# design/

@kazuemon/ui のデザイン原則を、候補の生成と選定を繰り返してブラッシュアップするための作業場所です。

## ディレクトリ構成

| パス               | 中身                                                                                                                                          |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `principles.md`    | 原則と判断の基準（なぜそうするか）                                                                                                            |
| `tokens.css`       | 現行版の値。Tailwind v4 の `@theme` にそのまま取り込める形式                                                                                  |
| `adr/`             | 決定の記録。1決定につき1本（書き方は [`adr/README.md`](./adr/README.md)）                                                                     |
| `adr/assets/`      | ADR に添える比較画像。1本につき1枚                                                                                                            |
| `references/`      | 原則の出どころになった参照画像（一覧は [`principles.md`](./principles.md#参照画像)）                                                          |
| `rounds/rNN/`      | 各ラウンドの候補定義（`candidates.mjs`）と、ラウンド開始時点の現行版（`base-tokens.css`）。生成物は `build/` に出力され、git では管理しません |
| `rounds/p1-final/` | 前半の最終版。候補を持たず、前半の決定をすべて反映した現行版だけを描きます                                                                    |
| `tools/`           | 候補の生成ツール、比較画像の撮影ツール、色の計算                                                                                              |

値は `tokens.css` の1か所だけに書きます。`principles.md` と ADR は、値ではなく役割トークン名（`--color-primary` など）で参照します。

## ループの全体像

ブラッシュアップは2段階で進めます。

|                   | 前半                                                        | 後半                                                   |
| ----------------- | ----------------------------------------------------------- | ------------------------------------------------------ |
| 目的              | ムードを決める                                              | 状態と動きを1軸ずつ詰める                              |
| 扱う軸            | トークン層と動き                                            | 画像と原則の食い違い、原則どうしの矛盾、残りの未決事項 |
| 1ラウンドで振る軸 | 未決の軸をすべて同時に                                      | 1軸だけ                                                |
| 提示方法          | design キャンバス                                           | Storybook                                              |
| 終了条件          | 子の4案が現行版に勝てないラウンドが2回続く（上限6ラウンド） | 軸のリストが空になる                                   |

前半の上限に達した場合は、残った軸を後半に回します。各軸の一覧は [`principles.md` の「軸の一覧」](./principles.md#軸の一覧) にあります。

## 進捗

- **前半: ラウンド4で終了（2026-09-12）**。前半の軸がすべて決まりました。決定は ADR-0005〜0018 にあります。結果は [`rounds/p1-final/`](./rounds/p1-final/) のキャンバスで確認できます
- **後半: 2026-09-12 開始**
  - `design/tokens.css` を `src/styles/globals.css` から `@import` しました。密度の寸法（`--size-control` など）は、globals.css が入力方式から解決します。`data-density` を付けた要素の中では、その密度に固定されます
  - 最初の部品として、Select と TextField を `src/components/` に作りました。ヘッドレス UI には Base UI（`@base-ui/react`）を使います。Select の選択肢（浮かぶ部分）の見た目は仮です
  - 軸 01「フォーカス時の変化量」は、A（塗りはグレーのまま、2px の枠線だけを足す）に決まりました（[ADR-0019](./adr/0019-field-focus-change.md)）
  - 軸 02「マウス用の寸法」は、B（高さ 40px・文字 14px）に決まりました（[ADR-0020](./adr/0020-control-size-fine.md)）。軸 01 でユーザーが「パソコン版の入力欄の高さが低すぎるかも」と指摘したため、エラー時の塗りより先に扱いました
  - 軸 03「エラー時の塗り」は、B（淡い赤の塗り＋赤の枠線）に決まりました（[ADR-0021](./adr/0021-field-error-fill.md)）
  - 軸 04「入力欄の hover」は、B（半段濃く）に決まりました。あわせて「hover は状態ではなく手応えなので、明度で表してよい」を原則にしました（[ADR-0022](./adr/0022-field-hover.md)）
  - 軸 05「Danger と Secondary の区別」は、A（Danger を `#BA012D` に濃くする）に決まりました（[ADR-0023](./adr/0023-danger-color.md)）。最初のボタン（`src/components/Button.tsx`）も、この軸のために作りました
  - 軸 06「グレーのボタンの塗り」は、現行版（`#EFF0F1`）のままに決まりました。入力欄の横に付くものは prefix・suffix としてグレーにし、白いボタンは影に輪郭を加えて作ります（[ADR-0024](./adr/0024-neutral-button.md)）
  - 軸 07「白いボタンの輪郭」は、A（1px の `#DEE0E1`）に決まりました。3:1 の輪郭も選べるようにしました（[ADR-0025](./adr/0025-surface-button-line.md)）
  - 軸 07 で、行の高さの端数のせいでボタンの文字が 1px ずれることが分かり、行の高さを整数 px にしました（[`principles.md` の「和文の縦位置」](./principles.md#和文の縦位置)）
  - 軸 08「Disabled の表し方」は、2回の比較で D2（色を持つ部品は薄く、入力欄とグレーのボタンは明るいグレー。ラベルとキャプションは変えない）に決まりました（[ADR-0026](./adr/0026-disabled.md)）。トグル（`src/components/Switch.tsx`）も、この軸のために作りました
  - 軸 09「平らなボタン・リンクの hover と押下」は、D2（枠線のボタン・リンクは文字の色を淡く敷き、文字のリンクは背景なし。押下で 1px 沈む）に決まりました（[ADR-0027](./adr/0027-flat-press.md)）。リンク（`src/components/Link.tsx`）も、この軸のために作りました。リンクの色・下線・大きさは仮です
  - 軸 10「各部品の既定の色」は、E（色を指定しないときは、色を持つ部品をすべてグレーにする）に決まりました（[ADR-0028](./adr/0028-default-color.md)）。色は部品ごとの `color`（primary / secondary / neutral）で選びます。タグ（`src/components/Tag.tsx`）も、この軸のために作りました。トグルには `color` を足しました。[ADR-0007](./adr/0007-tag-color.md) には、決まったのはタグの塗り方で、水色という色ではないことを追記しました
  - 軸 11「Disabled の詰め（トグル、グレーの枠線のボタン）」は、4回の比較で H（グレーの部品は、押せるときも押せないときも、グレーのボタンと同じ組み合わせにする）に決まりました（[ADR-0029](./adr/0029-disabled-refine.md)）。押せる OFF のトラックはグレーのボタンの塗り（`#EFF0F1`）、押せないグレーのトグルはトラック `#E1E3E4`・ノブ `#A3A5A6`、押せないグレーの枠線のボタンは文字 `#A3A5A6` です。押せないときの文字が枠線の色になる、枠線のボタンの不具合も直しました。[ADR-0011](./adr/0011-switch-off-track.md) と [ADR-0026](./adr/0026-disabled.md) には、この軸で変わったことを追記しました。軸 08 の比較のストーリーは、ADR-0026 のときの見た目で固定しました
  - 軸 12「文字のリンクの見た目」は、5回の比較で G2（ふだんは淡い下線、hover で下線だけを文字より濃くする。文字の色と背景は変えない）に決まりました（[ADR-0030](./adr/0030-text-link.md)）。「一旦これで」とのことなので、使ってみて違和感があれば見直します。リンク（`src/components/Link.tsx`）は、下線の色を通常と hover でトークンから読むようにしました。比較のために足した、下線を背景に描く仕組みは、F1〜F4（hover で下線が動きながら太くなる案）を比較のストーリーで再現するためだけに残しています。[ADR-0027](./adr/0027-flat-press.md) には、文字のリンクの hover がこの軸で決まったことを追記しました。軸 09 の比較のストーリーは、下線と角丸のトークンを書き足し、比べたときの見た目で固定しました
  - 軸 13「キーボード操作時のフォーカス」は、2回の比較で B（ボタン・リンク・トグルは、キーボードで操作したときだけ 2px の青い線を 2px 離して描く。動くのは線の色だけ）に決まりました（[ADR-0031](./adr/0031-focus-visible.md)）。入力欄は原則2の枠線のままです。フォーカスの線は `src/components/focus-styles.ts` にまとめ、ボタン・リンク・トグルで共有します。文字のリンクの角丸（`--link-text-radius`）を 0px から 6px に変え、[ADR-0030](./adr/0030-text-link.md) に追記しました。軸 12 の比較のストーリーは、角丸を 0px に固定しました。Select のボタンで、青い枠線にブラウザの既定の線が重なって出ていた不具合も直しました
  - 軸 14「和文の縦位置の補正」は、A（和文フォントの縦の寸法を、漢字の枠に合わせる）に決まりました（[ADR-0032](./adr/0032-text-offset.md)）。ユーザーは C（和文と英大文字の中間）も考えていましたが、困っていたこと（入力欄の和文が上に寄る、入力欄と Select で縦位置がずれる）だけを直す A になりました。IBM Plex Sans JP の `@font-face` に `ascent-override: 88%`・`descent-override: 12%`・`line-gap-override: 0%` を足し、英字フォント（Mulish）は元のままです。`@fontsource/ibm-plex-sans-jp` の CSS は書き換えられないため、`scripts/generate-fonts.mjs`（`pnpm run fonts`）で補正を足した `src/styles/ibm-plex-sans-jp.css` を作り、`src/styles/globals.css` から読むようにしました。`@fontsource/ibm-plex-sans-jp` を更新したら、流し直します。補正したあとは、入力欄でも div でも、和文の差が 0.00px になりました（[`principles.md` の「和文の縦位置」](./principles.md#和文の縦位置)）。軸 14 の比較のストーリーは、現行版の行を元の寸法で上書きして再現しています。入力欄に和文の値を入れているほかの比較のストーリーでは、値が約 1px 下がります。これまでの ADR の比較画像は、撮った時点の見た目のままです
  - 軸 15「塗りのボタンを押したときの縁」は、2回の比較で A（押しても輪郭の 1px の線を残し、消すのは落ち影だけ）に決まりました（[ADR-0033](./adr/0033-press-edge.md)）。軸 14 を比べている途中で、ユーザーが「グレーの方は小さくなる挙動も入っているように見えます」と気づいたことから始めた軸です。押すと影の中の輪郭の線まで消えるため、白地に近いグレーのボタン（`#EFF0F1`）は縁が1周内側に下がり、縮んだように見えていました。「一旦 A で進めます。」とのことなので、使ってみて違和感があれば見直します。押したときの影を `--shadow-raised-press`（hover と同じ輪郭の線）にし、ボタン（`src/components/Button.tsx`）はここから読むようにしました。グレーのボタンの影と枠線もトークンにしましたが、比べた案を比較のストーリーで再現するためのもので、値はほかの塗りのボタンと同じ影、枠線なしです。原則3の【固定】「影を完全に消すのは押下に取っておく」は書き直し、hover と押下は 1px 沈むことで見分けるようにしました。[ADR-0006](./adr/0006-button-shadow.md) には、押下の影を変えたことを追記しました
  - 軸 16「ボタンの送信中（loading）」は、5回の比較で、E を既定にし、B と C も選べる形に決まりました（[ADR-0034](./adr/0034-loading.md)）。送信中は押せないボタンと同じ見た目にし、印（回る円・線）だけは薄くしません。E はラベルを 55% に薄くして回る円を重ねる形、B はラベルの左に回る円、C は下端に流れる線です。回る円は元の文字の色、線はボタンの濃い色です。押せない見た目への切り替わりは F1（押下と同じ緩急で 0.2 秒。出るときも戻るときも）です。ボタン（`src/components/Button.tsx`）に `loading` と `loadingIndicator`（`overlay`・`inline`・`bar`）を足しました。送信中は `aria-disabled`・`aria-busy` を付け、`disabled` 属性は付けないので、フォーカスは外れません。押しても何もせず、フォームも送信しません。印を薄くしないため、ボタン全体の `opacity` は使わず、塗り・文字・枠線の色を Disabled の薄さで混ぜています。そのため、`disabled` を切り替えたときの色の変化も 0.2 秒で動くようになりました。比較のストーリーに、実装した Button を確かめる「実装した Button」を足しました。入力欄と Select の送信中、動きを減らす設定のときの回る円と線は、まだ決めていません。薄くなった色のボタンの上では、白い回る円が弱く見えます（白との比は約 1.7）
  - 軸 17「入力欄の prefix・suffix」は、3回の比較で、A（文字もボタンも入力欄の端に接した塊）を既定にし、B（内側に 4px 浮かせる形）も選べる形に決まりました。エラーのときは、塊の塗りを赤み（`#F1E0DE`）にし、文字の色は変えません（[ADR-0035](./adr/0035-field-addon.md)）。部品（`src/components/FieldAddon.tsx`）を先に作り、形と色をトークンで切り替えて比べました。TextField に `prefix`・`suffix`・`addonShape` を、Select に `prefix`・`addonShape` を足しました。端に接する形では、塊が本体の枠線の色を受け継ぐので、フォーカスとエラーの枠線が塊の外側にも続きます。suffix に入れるボタンは、パスワードの表示・非表示のように入力欄そのものを操作するものに限り、検索のように内容を送るボタンは欄の外に置くことにしました。[ADR-0024](./adr/0024-neutral-button.md) には、決定2の「検索ボタンなど」を見直したことを追記しました。目のアイコン（`EyeIcon`・`EyeSlashIcon`）も、この軸のために足しました。入力欄が出てくる ADR の比較画像を撮り直し、この軸の変更で見た目が変わっていないことを確かめました。suffix のボタンにキーボードでフォーカスしたときの見せ方と、文字の prefix の読み上げは、まだ決めていません
  - 軸 18「Select の選択肢」は、2回の比較で E1（白い面に 1px の細い境界線とやわらかい影。選んだ項目は淡い青）に決まりました（[ADR-0036](./adr/0036-select-popup.md)）。1回目は、影で浮かせる A、輪郭をはっきりさせる B、選んだ項目を淡い青にする C、欄と同じグレーの面 D を比べました。2回目は、A の影と C の青に、A と B の間の濃さの輪郭を合わせた E1〜E3 を比べました。輪郭は白いボタンの輪郭と同じ `#DEE0E1`、選んだ項目は青のタグと同じ組み合わせです。浮かぶ UI の影は、押せることではなく重なりを表すものとして、原則1に例外として足しました（「原則1もかなり最初の方に決めたので、例外があってもいいなあとは思います。」）。浮かぶ部分の見た目をトークン（`--select-popup-*`・`--color-select-*`）にし、Select（`src/components/Select.tsx`）はここから描くようにしました。Select に `open`・`defaultOpen`・`onOpenChange`・`modal`・`container`・`collisionAvoidance` を足しました。比較のストーリーで、開いたままの選択肢を行ごとに並べるために使っています。Select が出てくる ADR の比較画像を撮り直し、閉じた Select の見た目が変わっていないことを確かめました。開いた直後は、選んだ項目がキーボードで選んでいるのと同じ状態になり、一段濃い青で表示されます。開くときと閉じるときの動きは仮のままです。影のトークンは Select の選択肢だけのもので、ほかの浮かぶ UI で同じ影を使うかは、それぞれを作るときに決めます
  - 軸 19「Select のボトムシート」は、シートを6回、浮かぶ選択肢を3回比べ、指で操作していて画面が狭いときは選択肢を画面の下から出すシートにすることに決まりました。長い選択肢は、シートでも浮かぶ選択肢でも途中で切り、上下の端の印で続きを見せます（[ADR-0037](./adr/0037-select-sheet.md)）。シートは、出し方、長い選択肢の見せ方、続きの印の順に比べ、O（上の端は区切り線をいつも出し、上下の端に内側の影）に決まりました。右上に固定した ×、後ろを暗くする背景、ラベルとヘルプテキストを1つにまとめた見出しを付けます。長いときは画面の高さの約半分で開き、つまみで高さいっぱい（85%）まで広げます。浮かぶ選択肢は、S（画面の高さの半分。ただし 10.5 項目まで。上下の端に内側の影）に決まりました。項目の数の上限は「一旦」です。切り替えの基準は、最初は原則11のとおり画面の幅（640px 未満）としていましたが、ユーザーのメモ「シートを使う利点は、ユーザーの指の動きを緩和するためだと思っています。そうなると、幅が基準にはならないかもしれません。」を受けて、指で操作していて（`pointer: coarse`）、縦長なら 768px（`md`）未満、横長なら 1024px（`lg`）未満のときにしました。タブレットとマウスは浮かぶ選択肢のままです。原則11の「構造は画面幅」を Select について見直し、[ADR-0004](./adr/0004-density-and-structure-switching.md) に追記しました。Select（`src/components/Select.tsx`）に `presentation`（`auto`・`popover`・`sheet`。既定は `auto`）を足しました。あわせて足した `sheetDetent`・`sheetMoreCue`・`popoverMoreCue`・`popoverMaxHeight` は、既定が決まった形で、ほかの値は比べた案を比較のストーリーで再現するために残しています。シートは Base UI の Select の浮かぶ部分を画面の下に固定して作り、つまみを引く操作は自前です。× とつまみで閉じるため、開閉を部品の中でも持つようにしました。閉じる × のアイコン（`XIcon`）も足しました。トークンは `--select-sheet-radius`・`--color-select-sheet-backdrop`・`--color-select-sheet-edge-shadow`・`--shadow-select-sheet`・`--duration-sheet`・`--ease-sheet`・`--select-popup-max-rows` です。1回目で、開くと後ろの画面が上にずれることが分かりました（「ボトムシートが出てくるときにレイアウトシフトが起きて、フォーカスが解除されたり後ろのコンテンツが上にずれたりしますね。」）。比較用のスマートフォンの枠が `overflow: hidden` で、スクロールできる箱になっていたためです。開いた直後にフォーカスが枠の外にある選択肢へ移り、枠の中身がずれていました。枠を `overflow: clip` に直し、実際の画面の幅（375px）でもずれないことを確かめました。選んだあとに本体の青い枠線が一瞬消える不具合も直しました（「あと、選択肢を選んだ後に一瞬青の枠線が消えるのが気になっています。」）。Base UI が閉じる動きのあとでフォーカスを本体に戻すためで、フォーカスが戻るまで、開いているときの見た目を保ちます。外を押して閉じたときは保ちません。浮かぶ選択肢の上下の余白を選択肢の内側に移し、面を角丸で切り抜くようにしたので、[ADR-0036](./adr/0036-select-popup.md) の比較画像を撮り直すと、面の角の 18 ピクセルだけが最大 5/255 違います。見ても分からない差なので、画像はそのままにし、ADR-0036 に追記しました。軸 18 の比較のストーリーの開いたままの列は、比べたときの見た目（上限と影なし）で固定しました。入力欄と Select が出てくるほかの ADR の比較画像は、撮り直しても変わっていません。つまみを引く速さ（勢い）は見ないこと、開いたまま画面の大きさを変えても高さを計算し直さないこと、Provider で固定する仕組みがまだないこと、項目の数の上限が「一旦」であることは、まだ決めていません
  - 軸 20「Warning の扱い」は、1回の比較で A（黄色 `#EFF16B` は塗りに残し、上に載せる文字とアイコンは濃紺。白地の文字・枠線・アイコンは、同じ色相で暗くしたオリーブ色 `#727200`）に決まりました（[ADR-0038](./adr/0038-warning.md)）。タグは淡い黄色 `#F3F5CE` にオリーブ色の文字（4.55:1）です。値の層に `--palette-warning-700` を足し、`--palette-warning-50` を `#FCFCE1`（白地 1.04:1 で面が見えなかった）から `#F3F5CE`（ほかのタグの面と同じ明度）に変えました。役割の層に `--color-warning`・`--color-on-warning`・`--color-fg-warning`・`--color-tag-warning`・`--color-on-tag-warning` を足し、タグ（`src/components/Tag.tsx`）の `color` に `warning` を足しました。警告の色のボタンは、ユーザーのメモ「warning のボタンというのがそもそもあまり使うことは無いですが。」を受けて、いまは作りません。トークンはあるので、必要になったら Button の `color` に足します。入力欄の下の警告の文言とお知らせは、比較のストーリーの中で仮に組んだだけで、部品はまだありません。Success と Info の役割も比較のときに尋ねましたが、答えはなく、まだ当てていません
  - 軸 21「リンクの大きさ」は、枠線のリンクと文字のリンクを別のストーリーで比べ、1回でどちらも現行版のままに決まりました（[ADR-0039](./adr/0039-link-size.md)）。枠線のリンクは枠線のボタンと同じ寸法（`--size-control`・`--text-control`・`--space-control-x`。密度で切り替わる）で、押せる範囲は見た目と同じです。文字のリンクは大きさを持たず、置いた場所の文字のままで、押せる範囲も広げません。ユーザーのメモは「文字のリンクは現行版で良いです。領域に問題がある場合、ボタンを使う考えです。」でした。トークンは変えていません。リンク用の寸法（`--size-link` など）は、比較のストーリーの中だけで使いました。リンク（`src/components/Link.tsx`）は、「大きさは仮」のコメントを決定に書き換えただけです。幅いっぱいに広げた枠線のリンクの中身の寄せ方と、ページを移るリンクをボタンの形にする方法（いまの Button は `href` を持てない）は、まだ決めていません
  - 軸 22「prefix・suffix の残り」は、suffix のボタンのフォーカスと文字の prefix の読み上げを別のストーリーで比べ、1回でどちらも A に決まりました（[ADR-0040](./adr/0040-field-addon-rest.md)）。suffix のボタンにキーボードでフォーカスしているあいだは、欄の青い枠線を消し、ボタンのフォーカスの線（[ADR-0031](./adr/0031-focus-visible.md)）だけにします。エラーの赤い枠線は残し、入力欄にいるときとマウスで押したときは今と同じです。文字の prefix・suffix は、入力欄の説明（`aria-describedby`）の先頭につなぎ、見えている文字は読み上げから外します（`aria-hidden`）。比べたときの A は見えている文字を読み上げに残しており、前から順に読むと https:// を2回読むため、`aria-hidden` を足しました。ユーザーのメモは「一旦 A で行きましょう。」で、読み上げのやり方は「一旦」です。本体（`src/components/field-styles.ts`）に、中の `FieldAddonButton` が `:focus-visible` のあいだ枠線を透明にする指定を足しました。端に接する塊の枠線も一緒に消え、エラーの欄には効きません。TextField（`src/components/TextField.tsx`）は、文字の prefix・suffix に id を付けて説明の先頭につなぎ、キャプションとエラーの文はその後ろに続きます。`FieldAddon` に `aria-hidden` を渡したときはつながないので、読み上げを呼び出し側で決められます。比較のストーリーでは、この仕組みで現行版と B〜D を再現しています。ボタン（`FieldAddonButton`）と Select は変えていません。Chrome で測ると、`prefix="https://"` の TextField は、名前「サイトの URL」、説明「https:// プロフィールに表示します」で、単独の https:// は読み上げに残りません。見た目が変わるのは、ボタンにキーボードでフォーカスしたときの枠線だけで、ほかのストーリーの見た目は変わりません。[ADR-0035](./adr/0035-field-addon.md) の「分かっていること」のうち、ボタンのフォーカスと文字の prefix の読み上げは、この軸で決まりました。文字の suffix（「円」など）は prefix と同じ仕組みで、読み上げは後から測りました（下）。`FieldAddon` を別の部品で包んで渡すと、つながりません
    - 後から来たメモ「prefix は測ってください。」（prefix と同じ仕組みでつないだ suffix の文字のこと）を受けて、文字の suffix を Chrome で測りました。`suffix="円"` の TextField は、名前「月の予算」、説明「円 税込みで入れてください」で、単独の「円」は読み上げに残りません。prefix と suffix が両方あるときの説明は prefix → suffix → キャプション → エラーの文の順で、見た目の順とはキャプションの場所だけが違います。軸 22 のストーリーに suffix の例を足し、[ADR-0040](./adr/0040-field-addon-rest.md) に追記しました。比較画像は撮り直していません
  - 軸 23「ヘルプテキストとエラーを同時に出すか」は、2回の比較で、F（キャプションを本体の上に、アイコン付きのエラーか警告を本体のすぐ下に）を既定にし、F′（キャプションを本体の下に置き、その下にエラー・警告を足す）も選べる形に決まりました（[ADR-0041](./adr/0041-caption-and-message.md)）。1回目は、現行版（エラーがキャプションと入れ替わる）と A〜E を比べました。2回目に、ユーザーの案として F と F′ を足しました。キャプションはエラー・警告のあいだも消しません。エラーは丸の「!」と `#BA012D`、警告は三角と `#727200` で、形でも見分けます。警告は欄の見た目を変えず、両方を渡したときはエラーだけを出します（後から、両方出すことにしました。下）。原則4はキャプションを「本体の下」【固定】としていましたが、ユーザーのメモ「原則4ですが、元イメージのボタンキャプションはセンタリングされており、今回のキャプションは左寄せになっているという違いがあります。なので、許容する認識です。」を受けて、入力欄のヘルプテキストはボタンのキャプションとは別のものとして扱います。原則4の例外にはしていません。Field（`src/components/Field.tsx`）に `captionPlacement`（`top`・`bottom`。既定は `top`）と `warning` を足し、TextField と Select から渡せるようにしました。エラーは Base UI の `Field.Error`、警告は `Field.Description` で描くので、警告は説明につながりますが、欄はエラーの状態になりません。DOM の順を見た目の順にし、読み上げの説明も prefix・suffix → キャプション → エラー・警告の順にしました。Select は、本体のボタンにも `aria-describedby` を渡すようにしました。丸の「!」（`WarningCircleIcon`）と三角（`WarningIcon`）のアイコンを足し、アイコンに `className` を渡せるようにしました。トークンは `--field-message-icon-size`（キャプションの行の高さと同じ 16px）と `--field-message-gap`（4px）です。Chrome で測ると、`prefix="https://"`・キャプション・エラー付きの TextField の説明は「https:// プロフィールに表示します URL の形が正しくありません」です。エラー・警告が出ると高さが指用 24px・マウス用 22px 増え、本体の位置は F・F′ とも変わりません。[ADR-0038](./adr/0038-warning.md) で「部品がまだない」としていた入力欄の下の警告の文言は、この軸で作りました。軸 20 の比較のストーリーは、警告をキャプションの場所に仮に組んでいたので、その欄を `captionPlacement="bottom"` にして、比べたときの見た目（本体の下）のままにしました。ほかの比較のストーリーでは、キャプションが本体の上に、エラーがアイコン付きになります。ADR の比較画像は比べたときの記録なので、撮り直していません。エラー・警告の行が出たときに読み上げで知らせないこと（`aria-live` はない）と、Select のボトムシートにエラー・警告を出さないことは、まだ決めていません
    - 後から来たメモ「エラーと警告が両方あるなら、両方出したいですね」を受けて、両方を渡したときは、エラーの行、その下に警告の行を出すようにしました。行の間は `--space-field-gap`（指用 8px・マウス用 6px）、読み上げの説明はキャプション → エラー → 警告です。行ごとに polite の箱と 0.2 秒の開閉を持つので、エラーから警告に替わるときは、エラーの行が閉じて警告の行が開きます。Select のシートの見出しにも両方を出します（行の間 4px）。軸 23 に「エラーと警告の両方」のストーリーを足し、[ADR-0041](./adr/0041-caption-and-message.md) に追記しました。比較画像は変わりません
  - 軸 24「入力欄と Select の送信中」は、入力欄と Select の待っているあいだを2回、動きを減らす設定のときの印の動きを別のストーリーで比べ、止める形（E）と止めない形（G）を選べる形に決まりました（[ADR-0042](./adr/0042-field-loading.md)）。1回目は、現行版と A〜D を比べました。ユーザーのメモ（印は回る円と線から選びたい、止めるか止めないかも選びたい、止めるときは ▼ を隠すか薄くしたい）を受けて、2回目に E・F・G を足しました。止める形（`loadingBehavior="blocking"`）は、押せない欄と同じ見た目（塗り `#E1E3E4`・文字 `#A3A5A6`）にし、印は薄くしません。書き換えられず、Select は開けません。フォーカスは外さず、フォーカスの青とエラーの赤の枠線は残します。Select は、プレースホルダの場所に「読み込んでいます」を出し、▼ を隠して、回る円を ▼ のあった場所に置きます。止めない形（`"non-blocking"`）は書き換えられ、Select は「選んでください」がすぐ出て開けます。開くと、選択肢の下に「読み込んでいます」の行が出ます。回る円は ▼ の左に 8px 空けて置きます。既定は止めない形です（「既定はGでおっけーです。」）。印は回る円が既定で、下端の線も選べます（`loadingIndicator`）。動きを減らす設定では、回る円は3秒で1周（A）、線は流さずに幅いっぱいに引いて明滅（B）にし、ボタン・入力欄・Select の読み込み中の行のすべての印に当てます。ユーザーのメモは「ブロッキングの場合はEで、ノンブロッキングの場合はGで。」「loading は、スピナーがデフォルトで行きます。」「動きを減らす設定は、円はAのゆっくり案、バーは明暗かなと思いました。」でした。回る円と線は、ボタンから `src/components/Loading.tsx` に移して共有にしました。Field（`src/components/Field.tsx`）に `loading`・`loadingBehavior` を足し、TextField と Select に `loading`・`loadingBehavior`・`loadingIndicator` を、Select には `loadingText`（既定「読み込んでいます」）と `disabledIcon`（`show`・`hide`）も足しました。止めるときは、TextField に `readOnly` と `aria-disabled` を付け、`disabled` 属性は付けないので、フォーカスは外れません。Select は、Base UI の `readOnly` だけでは開けてしまうので、閉じたままに固定します。読み込み中の行は、一覧（`role="listbox"`）の中には選択肢しか置けないので、一覧のすぐ下に `role="status"` で置きます。トークンは `--animate-spin-reduced`・`--animate-loading-bar-reduced`（`@keyframes loading-bar-pulse`）です。押せない Select の ▼ は、これまで `#525C60` のままでした。後から来たメモ「すべて 6E787D に揃えたいです。」を受けて、プレースホルダの場所に出す文（ふだんの文・押せないときの理由・止めるときの「読み込んでいます」）と同じ `#6E787D`（押せない塗りの上で 3.51:1）にしました。押せない文字の色 `#A3A5A6` は選んだ値・入力した値だけに使い、値が入った押せない欄と、文を出している欄を見分けます。▼ の色はトークン `--color-select-icon-disabled` です。軸 08・17 の比較のストーリーでは、押せない Select の ▼ の色だけが `#525C60` から `#6E787D` に変わります。比較の枠（`design/stories/Comparison.tsx`）の `pick` に、カンマで区切って2つ以上の案を付けられるようにしました（`E,G`）。比較のストーリーの E・G の行は部品で描き、「フォームを送っている」の列だけは比べたときの組み立てのままです。動きを減らす設定のストーリーには、決まった形を E として足しました。ほかの行は、設定がオンでも比べたときの動きになるよう戻しています。Chrome で測ると、動きを減らす設定の回る円は1周 2.97〜2.98 秒（ふだんは 0.99 秒）で、線は幅いっぱいのまま濃さが 0.20 → 0.60 → 0.20 と 1.6 秒で1往復します。軸 16・19・23 の比較のストーリーの見た目は変わりません。[ADR-0034](./adr/0034-loading.md) の「分かっていること」に残した2つ（動きを減らす設定と、入力欄や Select の送信中）は、この軸で決まりました。フォーム全体を送っているあいだの形（欄を止めて、印はボタンだけ）と、それを欄に配る仕組み、読み込み中の行と浮かぶ選択肢の高さの上限、ボトムシートでの読み込み中の行の見た目は、まだ決めていません。止めているあいだの suffix のボタンと、動きを減らす設定で回る円がふわっと出る動きは、後から決めました（下）
    - 後から来たメモ「待っている間も suffix は押せていいと思います」「動きを減らす設定ではふわっとでる動きはなしでよいです」を受けて、止めているあいだも suffix のボタンは押せるまま（見た目もふだんのまま。部品は変えていない）にしました。動きを減らす設定では、`--animate-loading-in` を `none` にし、回る円をすぐに出します（`tokens.css` の `@theme` の外に置いた `@media`。`@theme` の中には `@media` を書けないため）。いまこの動きを使うのはボタンの印だけです。[ADR-0042](./adr/0042-field-loading.md) に追記しました。比較画像は変わりません
  - 軸 25「お知らせと状態の色」は、状態の色とお知らせの形を別のストーリーで比べ、1回で決まりました。状態の色は C（淡い面）を既定にし、D（濃い塗り）と、形の E（枠線）も選べます。形は A（縦に積む）です（[ADR-0043](./adr/0043-notice.md)）。ユーザーのメモは「25 については C がデフォルト、D も選べるようにしたいです。あと、お知らせの形で言及されていた、アウトラインタイプも選べたら嬉しですね。warning の原則については、こちらに変更で良さそう。」と「お知らせの形は、めっちゃ A が好みです。C のようにボタンも置けるようにはしたいですが、余白類は A に揃える形で。」でした。お知らせの部品（`src/components/Notice.tsx`）を作り、`tone`（`info`・`success`・`warning`・`danger`）、`appearance`（`soft`・`filled`・`outline`。既定は `soft`。ボタンの `appearance` と同じ名前）、`title`、`children`（本文）、`actions`、`onClose` を持たせました。淡い面は、タグと同じ塗り方です。題とアイコンは同じ色相の濃い色、本文は濃紺です。情報は Primary と同じ青（`#196BD6`・`#2474DF`）で、値の層の Info（`#6B9FF1`）には役割を当てません。成功は `#008132` です。形は、アイコンの右に太字の題・本文・操作を縦に積み、× を右上に置きます。角丸は部品と同じ 12px、余白と文字は部品の寸法で、密度で変わります。操作には、白いボタンか文字のリンクを本文の下 6px に置けます。余白は A のままで、C の白いボタンだけを取り込みました。お知らせには影を付けず、影があるのは中の白いボタンだけです。塗りのお知らせの中では、フォーカスの線をお知らせの文字の色（白か濃紺）にします。青い線が情報・成功・危険の塗りの上で 1.00〜1.48:1 しかないためで、[ADR-0031](./adr/0031-focus-visible.md) に例外として追記しました。警告のお知らせの既定は、黄色の塗りから、淡い黄色 `#F3F5CE` にオリーブ色の題とアイコンの形に変わりました。黄色の塗りは `filled` として残ります。[ADR-0038](./adr/0038-warning.md) には、このことと、お知らせ用の役割を塗りのトークンと分けたことを追記しました。読み上げは、危険が `role="alert"`、ほかが `role="status"` で、× は箱の外に置いて「閉じる」と読みます。値の層では、`--palette-success-50` を `#E1FCE7` から `#E5F8E7`（ほかの淡い面と同じ明度）に変え、`--palette-success-700`・`--palette-red-75` を足しました。役割の層には、`--color-fg-info`・`--color-fg-success` と、お知らせの役割（`--color-notice-*`・`--color-on-notice-*`）、形の `--notice-radius` を足しました。丸の「i」（`InfoIcon`）と丸のチェック（`CheckCircleIcon`）のアイコンも足しました。比較のストーリーの採用した行（状態の色の C・D、形の A・E）は部品で描きます。比べたときのストーリーの中の Notice と画素で比べ、差が文字の縁だけであることを確かめました。形の比較画像の色は、比べたときの既定（B）のままなので、A の警告は黄色の塗りです。4色 × 3つの見た目を並べる「実装した Notice」のストーリーも足しました。Chrome で測ると、お知らせの中の 60 か所のフォーカスの線は、淡い面と枠線で青、塗りで白か濃紺でした。390px の幅でも横にスクロールしません。入力欄の下の成功・情報の行、警告も `alert` にするか、あとから出すお知らせを読み上げで確実に知らせる仕組みは、まだ決めていません。タグの成功・情報・危険の色は、後から足しました（下）
    - 後から来たメモ「状態のタグは欲しいです！」を受けて、タグ（`src/components/Tag.tsx`）の `color` に `info`・`success`・`danger` を足しました。面と文字は淡い面のお知らせの面と題と同じ値で、トークンは `--color-tag-info`・`--color-on-tag-info`・`--color-tag-success`・`--color-on-tag-success`・`--color-tag-danger`・`--color-on-tag-danger` です（値の層は足していません）。情報のタグは青のタグと同じ値（「情報は前の色合いでよいです」）、危険のタグの面はピンクのタグの面とほぼ同じ（色の差 0.6）、成功と警告の文字は明度の比 1.02:1 で、どれも受け入れました（「成功と警告のいろ、そこまでちかいですか？これでいいかなと」）。`filled` の名前、お知らせの中のリンクの色、塗りの警告の中の濃紺のフォーカスの線も、そのままです（「filled でいいですね」「お知らせの色合わせもよし」「警告の色は外れてなければおまかせします。」）。軸 25 に「実装した状態のタグ」のストーリーを足し、[ADR-0043](./adr/0043-notice.md) に追記しました。比較画像は撮り直していません（値が同じなので見た目も同じ）。警告も `alert` にするかについては、「危険だけ割り込み、というのは一般的ですか？」と尋ねられています
  - 軸 26「リンクの残り」は、幅いっぱいの枠線のリンクとボタンの形のリンクを別のストーリーで比べ、1回で決まりました（[ADR-0046](./adr/0046-link-rest.md)）。幅いっぱいの枠線のリンクは、A（文字とアイコンをまとめて中央）を既定にし、B（文字は左、最後のアイコンは右端）と C（文字は箱全体の中央、最後のアイコンは右端）も選べます。ボタンの見た目のリンクは、B（Button の render）で作ります。ユーザーのメモは「26の幅いっぱいのリンクについては、A・B・C を全部選べるにしたいですね… デフォルトは A でよさそう。」「26について、塗のボタンのリンクも欲しいです！リンクのものもボタン（onClick など）のものは見た目は同じです！（機能的に button がもとなのか a が元なのかだけ） Nextjs 対応はしたいので render のパターンでお願いします。」と「あ、ボタンの見た目のリンクの場合、右上向き矢印は必ず入る、という認識です。」でした。リンク（`src/components/Link.tsx`）に `contentAlign`（`center`・`between`・`center-end`。既定は `center`。枠線のリンクだけに効く）と `render` を足しました。`between`・`center-end` では、最後の要素（アイコン）の前を `data-slot="link-label"` の包みに入れ、入りきらないときは「…」で切ります。`center-end` の左の余白は、左右の余白＋アイコン＋8px（マウス用 36px・指用 44px）で、最後にアイコンがあるときだけ足します。ボタン（`src/components/Button.tsx`）に `render` を足し、`<a href>` や Next.js の Link を渡すと、同じ見た目の `<a>` で描きます（Base UI の `useRender`）。塗り・枠線のすべての色をリンクにできます。リンクのときは、ラベルの後ろの最後に ↗（`ArrowUpRightIcon`。飾りなので読み上げない）を必ず足し、`target="_blank"` なら読み上げだけの「（新しいタブで開きます）」と `rel="noopener noreferrer"` を足します。`loading`・`loadingIndicator`・`type` は無視し、開発時に1回だけ警告します（後から、型でも止めました。下）。hover と押下の条件を `enabled:` から `not-disabled:` に書き直し、`<a>` にも効くようにしました。`<button>` の見た目と動きは変わりません。Link と Button で共有する小物は `src/components/link-parts.ts` に置き、↗ のアイコン（`ArrowUpRightIcon`）も足しました。トークンは変えていません。Chrome で測ると、Button の9つの見た目 × 2つの密度で、リンクの大きさ・計算された見た目・hover と押下はボタンと同じで、画素の差は文字の縁だけ（ページの上の置き場所による描き方の差）でした。読み上げは `link「実績を見る」`、新しいタブで開くリンクは `link「GitHub （新しいタブで開きます）」` です。既定が `center` になったので、幅いっぱいの枠線のリンクは中央に寄ります。軸 21 の文字のリンクのストーリーの「一覧の下に置く」の列は、左寄せから中央寄せに変わりました。ADR-0039 の比較画像は比べたときの記録なので、撮り直していません。ボタンの形のリンクの比較画像は比べたときの組み立てのままなので、↗ は外へ出るリンクにだけ付いています。決めたあとの形は「実装したリンク」のストーリーで見られます。実装のあと、提案した3つのルールを確かめてもらい、どれも決まりました。キーボードはリンクのままで、Enter で移り、Space では移りません。リンクにも押せない状態（`disabled`）を持たせ、`href` を外した `<a role="link" aria-disabled="true">` にします。渡した要素（Next.js の Link）は描かず、Tab では止まらず、読み上げでは「リンク、利用不可」と読まれます。見た目は、ボタンの見た目のリンクが `<Button disabled>` と同じ（↗ は残す）、文字・枠線のリンクが押せないグレーの枠線のボタンと同じ色です（後から、文字のリンクはただの文字の見た目に変えました。下）。新しいタブで開くリンクには、どの種類でも読み上げに「（新しいタブで開きます）」と `rel="noopener noreferrer"` を足します。↗ は、文字のリンクでは新しいタブのときだけ部品が文字の後ろに付け（最後の語と一緒に折り返す）、枠線のリンクではアイコンを置いたときはそれを使い、アイコンがない新しいタブのときだけ部品が付けます。ユーザーのメモは「Entry と Space に関しては理解できました。このままでおっけーです。」「おすすめしてもらったもので良さそうです。」「1はそのままでおっけーです。2は同じ形にしたいですね。文字のリンクの場合は、新しいタブのみ矢印アイコンを入れたいですが、枠線のリンクとはどれの話ですか？」と「ああー、理解しました。枠線のリンクはアウトラインボタンの見た目のボタンとほぼ似てますね。こちらはアイコンの指定があればそれを、指定がない新しいタブなら文字のリンクと同じ挙動で。」でした
    - 後から来たメモ「押せない文字のリンクはそもそも下線が引かれず、ただの文字と同じ見た目で。枠線のリンクは26の実装したリンクにあるものでよいです」を受けて、押せない文字のリンクは、下線と ↗ を付けず、色とカーソルを周りの文字から受け継ぐようにしました。押せない枠線のリンクは、いまのままです。「ボタンの見た目のリンクは loading を受け取らないのが正しそうですね（リンク(nextlink, a)に loading という状態はなさそう）」を受けて、Button の props の型を `ButtonProps`（ボタン）と `ButtonLinkProps`（`render` あり。`loading`・`loadingIndicator`・`type` は渡せない）に分け、`render` と一緒に書くと型のエラーになるようにしました。型を外して渡されたときの警告は残します。「文字のリンクはどこで見れますか？」「枠線のリンクの中身が〜をアイコンと〜はどこで見れますか？」に答えて、「実装したリンク」のストーリーに「文字のリンク」の区画と、枠線のリンクの「子が1つだけのとき」を足しました。[ADR-0046](./adr/0046-link-rest.md) に追記しました。比較画像は変わりません
  - 軸 27「エラー・警告の知らせ方」は、出たときの知らせ方と下の内容の動き（1回目）、Select のボトムシートの文と選択肢に付く文（2回目）を別のストーリーで比べて、すべて決まりました（[ADR-0044](./adr/0044-message-announce.md)）。送信したときは、エラーのある最初の欄へフォーカスを移す E を既定にし、フォームの上にエラーの一覧を出して一覧へフォーカスを移す C も選べます。欄を離れたときやあとから確かめたときは、行を polite で知らせます（A）。下の内容の動きは B（行の高さと濃さを 0.2 秒で開き、閉じる）です。ユーザーのメモは「27の知らせ方は、どういう風に読むのが一般的か教えてください。内容の動きは B が好みです。」と「27 については、お勧めいただいたものを実装してみてもらってもいいですか？ C のケースも選べるようにはしたいかもですね。」でした。一般的な読み方として、多くの React のフォームは送信したときに最初のエラーの欄へ移ること（E）、長いフォームではエラーの一覧を出す形も多いこと（C。GOV.UK と W3C の手引き）、欄を離れた直後に割り込む形（B）は避けることが多いことを答え、送信は E、離れたときは A、長いフォームでは C も選べる形を勧めました。フォームの部品（`src/components/Form.tsx`）を作り、`errorSummary` と `errorSummaryTitle`（既定「入力を確かめてください（N件）」）を持たせました。`noValidate` は既定で true です。送信すると、アプリの `onSubmit` が各欄の `error` を決めた描画のあとで、最初のエラーの欄（DOM の順）へフォーカスを移し、入力した文字を選びます。`errorSummary` のときは、危険のお知らせ（`soft`）の見た目で一覧を出し、`role="group"`・`tabindex="-1"` の一覧へフォーカスを移します。リンクの文はエラーの文そのもので、押すとその欄へ移ります。一覧は欄のエラーに合わせて直り、すべて直すと閉じます。警告は入れません。Base UI の Form は包んでいません。送信のときに Base UI の確かめ方で先に判定し、アプリが確かめる前にフォーカスを動かしてしまうためです。Field（`src/components/Field.tsx`）は、エラー・警告の行を、いつも置く `aria-live="polite"` の箱に入れました。送信で出た行は、その文が変わるまで箱を `aria-live="off"` にし、フォーカスの移る先の読み上げと二重にならないようにしました。行は Base UI の `Field.Error`・`Field.Description` ではなくふつうの要素で描き、閉じるあいだは最後の文を残して、読み上げからは隠します。お知らせ（`src/components/Notice.tsx`）には `live`（既定 true）を足し、一覧では role の箱を使いません。トークンは `--duration-field-message`（200ms。緩急は `--ease-press`）です。Chrome で測ると、E の送信ではフォーカスがメールアドレスの欄に移り、説明は「ログインに使います メールアドレスの形が正しくありません」で、送信で出た4つの行の箱は off でした。C では一覧の名前が「入力を確かめてください（3件）」で、欄を直すと 3件 → 2件 → 1件と減りました。行は 212ms で 22px まで開き、193ms で閉じます。キャプションと本体は動きません。動きを減らす設定では、最初のフレームで切り替わります。部品の行はどのストーリーでも polite の箱に入り、0.2 秒で開閉します。止まった見た目は変わらないので、ADR の比較画像は撮り直していません。[ADR-0041](./adr/0041-caption-and-message.md)・[ADR-0043](./adr/0043-notice.md) の「分かっていること」に、この ADR へのつなぎを足しました。2回目は、Select のボトムシートの文と、選択肢に付く文を比べました。シートの文は A（見出しのヘルプテキストの下に、本体の下と同じ行）が好まれました（メモ「シートの文は A が良いなと思いましたが、…」）。そこで、選択肢に関わる文と、選択肢に関係ない欄の文を分け、選択肢に付く文を別のストーリーで比べて、A（選べない選択肢は選べなくし、ラベルの下に灰色の理由。選べるが知っておくことがある選択肢は、三角と警告の色の2行目）に決まりました。ユーザーのメモは「27 は A が良さそうです。選択肢が選べない理由は複数あり、グルーピングは難しいと思いました。ただし、テキストの生成ロジックは、実装ガイドラインベースとし、コンポーネントレイヤーでは持たなくてよさそうです。」と「選べない選択肢は止まって読み上げて良さそうです。2と3は許容します。」でした（2は浮かぶ選択肢が本体の下の文を隠すこと、3は2行目の分だけシートが半分の高さで開くこと）。Select（`src/components/Select.tsx`）の `items` に `disabled` と `note`（`kind`: `reason`・`warning`、`text`）を足しました。選べない選択肢は Base UI の `Select.Item` の `disabled` で、ラベルは `#A3A5A6` です。マウスの hover では塗らず、矢印キーでは止まり、文字を打って探すときは飛ばします。キーボードで止まったとき（focus-visible）だけ hover と同じ塗り（`#F2F4F4`）を付けました。比べたときの A にはなかったもので、塗らないといまどこにいるか見えないためです。2行目は、理由が `#6E787D`（アイコンなし）、警告が三角と `#727200` です。2行目のある選択肢だけ上下 6px 高く（指用 52px・マウス用 48px）なります。名前はラベルで、2行目は説明として読まれます。シートの見出しのヘルプテキストの 4px 下に欄のエラー・警告の行を足し、一覧（listbox）の説明にもつなぎました。浮かぶ選択肢には欄の文を出しません。シートの半分の高さと浮かぶ選択肢の上限は、項目ごとの高さで数えるようにしました。文は呼び出し側が渡し、部品は組み立てません（`items`・`error`・`warning` の説明に書きました）。トークンは `--color-select-item-reason`・`--color-on-select-item-disabled` です。Chrome で測ると、選べない八王子市は option・名前「八王子市」・説明「お届けできません」・disabled で、Enter を押しても選ばれませんでした。シート（390×844）で選ばずに送信すると、見出しが 78px から 98px になり、一覧の説明は「お届けは23区内だけです 市区町村を選んでください」でした。「実装した Form」の市区町村は、八王子市・町田市を選べなくし、荒川区に警告の2行目を付けました。選べなくしたので、お届けできない地域のエラーは消しました。選んだ選択肢の警告を欄の `warning` にも渡すと、シートでは2行目と見出しの両方に出ます。欄に渡すかどうかは、実装ガイドラインで決めます。あわせて、軸 25 の形のストーリーの既定の状態の色を C に直し、[ADR-0043](./adr/0043-notice.md) の画像の説明を合わせました。あとから（非同期で）出たエラーへフォーカスを移すかは、まだ決めていません（一覧のリンクの文の欄の名前は、後から足しました。下）。本物の読み上げソフト（VoiceOver・NVDA）では試していません
    - 後から来たメモ「errorSummary にエラーの項目名は欲しいかも。あと、エラー間の間隔がかなり狭いので、ちょっと広げたいですね」を受けて、一覧のリンクの文を「欄の名前: エラーの文」にし（名前は欄のラベルの文字。Field のラベルに付けた `data-slot="field-label"` から読む）、項目の間と、題と最初の項目の間を `--space-field-gap`（指用 8px・マウス用 6px）に広げました（項目の間は、これまで 0px）。一覧の見た目と直すたびに減ること、題の文、最初の1件へのフォーカス、行の動きの 0.2 秒、選べない選択肢のキーボードの塗り、選んだ選択肢の警告がシートの見出しにも出ることは、確かめてもらい、そのままです。自前の Form は「一旦おｋ」で、あとで見直します。「実装した Form」のユーザー名に大文字の警告を足し、エラーと警告が両方出るようにしました。[ADR-0044](./adr/0044-message-announce.md) に追記しました。比較画像は変わりません
  - 軸 28「指用の高さ」は、スマートフォンで押して比べるストーリーと一覧で、現行版（44px）と A（40px）・B（48px）・C（52px）を比べ、1回で現行版の 44px に決まりました。C の 52px は「大きい指用」として残し、要素に `coarse-large` のクラスを付けたときに使います（[ADR-0045](./adr/0045-coarse-size.md)）。マウス用は 40px のままで、クラスを付けても変わりません。ユーザーのメモは「28 について、指用は 44px だなあと思いました。と思いつつ、ユースケースによっては 52px を使いたい場合もあるかなと思いました。全体テーマ設定のような形で、class を付けることで変化する、とかだとありがたいなと思います。PC についてはいまのままで行きましょう。」でした。クラスは `html`（アプリ全体）にも途中の要素（その中だけ）にも付けられ、`--size-control` を読むもの（ボタン・入力欄・Select の本体と選択肢の行・シートの見出しと ×・枠線のリンク・トグルの行）の高さが変わります。文字・行の高さ（52px で上下 14px）・左右の余白・並べる間・文字のリンクは変わりません。決めたあとのメモ「52px のときは、トグルは比例して大きくなるようにしたいですね。タグはインタラクティブな要素ではないので、あくまでも文字サイズ依存になりそうです。」を受けて、大きい指用ではトグルのトラックとノブも高さに比例して大きくしました（48×28px・ノブ 22px から 56×32px・ノブ 26px。`--switch-*-coarse-large`）。タグは文字の大きさに従い、変わりません。クラスの名前は「必要になったら変えましょう」で `coarse-large` のまま、ツールバーの切り替えも「このままで」残します。`tokens.css` の `--size-control-coarse` から「実機で詰める」を外し、`--size-control-coarse-large`（52px）を足しました。途中の要素に付けても効くように、`src/styles/globals.css` の密度の規則に `--density-coarse`（指用 1・マウス用 0）を足し、`.coarse-large` はその要素で `--size-control` を計算し直します。`.coarse-large` は、`html` に付けたときも `tokens.css` の `:root` に勝つよう、`@layer` の外に置きました。Storybook のツールバーに「指用の高さ」（44px・52px）を足し、`html` にクラスを付けられるようにしました。Chrome で測ると、指で操作している環境（390×844）では、クラスがないと 44px、`html` か途中の要素に付けると 52px、`data-density="fine"` の中は 40px です。マウスで操作している環境では、クラスを付けても 40px です。軸 19・24 のストーリーに付けても、ページは画面の幅からはみ出さず、縦に伸びるだけです。大きい指用で文字や余白も変えるか、どんな場面で使うかは、まだ決めていません。途中の要素に付けたときは、Select の選択肢（`body` の直下に出る）は `container` をその中にしないと 44px のままです
    - 大きい指用のトグルの大きさは、後から「トグルの大きさは今で OK です」と確かめてもらいました
  - 軸 20〜28 のあと、比べずに選んだことと残したことを尋ね、その答えを上の各軸に「後から来たメモ」として足しました。あわせて、軸を2つ足しました（「開閉の動きと Tag の整理は軸に足してください。」）
  - ADR の比較画像は、[`tools/capture-story.mjs`](#storybook-のストーリーを撮る後半) でストーリーを撮ります
  - ADR の比較画像は、軸を決めたときの記録で、あとの決定に合わせて撮り直しません（[`adr/README.md`](./adr/README.md) の書き方の規則）。例えば ADR-0019〜0024 の画像は、[ADR-0032](./adr/0032-text-offset.md)（和文の縦位置の補正）より前に撮ったので、今の見た目とは少し違います。撮り直すかは任されました（「とりなおしはおまかせします」）
  - **次にやること**: 軸 28 まで（[ADR-0045](./adr/0045-coarse-size.md)）と、軸 20〜28 の後から来たメモが反映できました。次の軸は、ユーザーが選びます。候補は、[`principles.md` の「後半の軸」](./principles.md#後半の軸storybook-で1軸ずつ詰める) に足した「浮かぶ選択肢の開閉の動き」と「Badge と Tag の整理」、Select の残り、各軸の残り（決めていないこと）です

## 前半の1ラウンド

### 候補の構成

1ラウンドで **4候補＋現行版** を並べます。探索範囲が狭くなったら、候補数を減らしてかまいません（ラウンド4は3案＋現行版）。1候補は次の2つで構成します。

- **見本シート**: Button、TextField、Select、Switch、Tag、Card を、指用とマウス用の2つの密度で左右に並べたもの。2密度の差は仮の値（44px／36px）で固定し、前半では振りません
- **画面1枚**: Biography 風のモバイル画面。編集できる欄と読み取り専用の欄を混在させ、原則7・8・9をまとめて確かめます

画面のレイアウト層の装飾（背景の大きな文字など）は全候補で共通にし、比較の変数にしません。

各候補の artboard には、その候補の**トークン表と規則表**を併記します。選んだ時点で、何を保存するかが確定するようにするためです。

### 候補の作り方（進化的な絞り込み）

1. 初回は、名前の付く方向を4案作り、広く散らします
2. 2回目以降は、現行版を親にして、**まだ決まっていない軸だけ**を変えた子を4案作ります
3. 変える幅は、ラウンドを追うごとに狭めます

決まった軸は親から引き継がれ、固定されます。

### 一貫性の担保

- **候補の中**: 1候補を1つのトークン表と1つの規則表から生成します。規則を破る候補や、コントラスト基準（[ADR-0002](./adr/0002-contrast-and-foreground-tokens.md)）を満たさない候補は出しません
- **ラウンド間**: 毎回、現行版を候補と並べて置きます。新しい候補を選んだ結果、前の決定と矛盾する場合は、その場で指摘します

### 選び方

ユーザーは、[判断の基準](./principles.md#判断の基準)に最も近い候補を1つ選び、一言メモを添えます（例: 「B。角丸が好き」）。一貫性は生成の仕組みで担保しているので、判断するのは基準への近さだけです。

複数の候補から部分ごとに選んでもかまいません（例: 「影は A、角丸は C」）。その場合は、部分ごとに ADR を書きます。

キャンバス上で候補に手を加えた場合は、その差分も「保存すべき特徴」として記録します。

全候補がどれも違うと感じた場合は、全却下にします。その場合も「この方向は違うと分かった」ことを反例として記録します。

## 後半の1ラウンド

- 1軸につき1本のストーリーを `design/stories/axis-NN-*.stories.tsx` に置きます。Storybook では `Design Review/NN 軸の名前` に並びます
- 現行版と候補を行に、状態（通常・フォーカス中など）を列に並べます。枠は `design/stories/Comparison.tsx` です
- 候補は、`design/tokens.css` への上書き（CSS 変数）だけで作ります。部品のコードは候補ごとに分けません。前半と同じく、候補の中の一貫性を仕組みで担保するためです。今のトークンで表せない案が必要になったら、先に部品をトークンで表せる形に直します
- 候補は、全案（現行版を含む）で軸の値を明示します。決まって `tokens.css` を更新した後も、同じ比較を再現するためです。前半の `base-tokens.css` の代わりです
- 決まったら、ストーリーの `pick` の既定値を採用した案にし、説明の先頭に決定と ADR の番号を書きます。ストーリーは記録として残します
- hover やフォーカスのように、操作しないと出ない状態は、`storybook-addon-pseudo-states` で固定して並べます。「通常」の列の部品は実際に操作できるので、動きも確かめられます
- 指用とマウス用の密度は、ツールバーの「密度」で固定して比べます（[ADR-0004](./adr/0004-density-and-structure-switching.md)）
- 密度の差は実機で指で押して詰めます
- 選び方は前半と同じです。ユーザーはストーリーを開き、1案を選んで一言添えます

## ツールの使い方

### ラウンドを始める

1. `design/tokens.css` を `design/rounds/rNN/base-tokens.css` にコピーして固定します。ラウンドの記録（候補の見た目や比較画像）を、後から同じ条件で再現するためです。以降、`tokens.css` を更新しても、そのラウンドの候補は `base-tokens.css` を土台に描かれます
2. `design/rounds/rNN/candidates.mjs` に候補を書きます。各候補は `base-tokens.css` への上書きとして定義します
3. 生成します

   ```sh
   node design/tools/generate-round.mjs design/rounds/rNN
   ```

   `design/rounds/rNN/build/` に、キャンバス用の artboard（`*.dc.html`）、`canvas.json`、ローカル確認用の `preview.html` が出力されます。コントラスト基準を満たさない候補があると、どの組み合わせが何対何で足りないかを表示して中止します

`candidates.mjs` の `candidates` を空にすると、現行版だけを描きます。前半の最終版（`rounds/p1-final/`）はこの形です。キャンバスの説明の付箋は、`spec.brief` で差し替えられます。

### 比較画像を撮る

ADR に添える比較画像は、次のコマンドで撮ります。現行版と全候補の同じ部品を横に並べ、`--pick` で指定した案に「採用」の印を付けます。

```sh
node design/tools/compare.mjs design/rounds/rNN \
  --parts <buttons|fields|switches|tags|card をカンマ区切り> \
  --pick <採用した案の ID> \
  --title "ADR-NNNN タイトル" \
  --out design/adr/assets/NNNN-kebab-title.png
```

- `--height` で画像の高さを調整します（既定は 640px）。部品が多いときは大きくします
- `--note` で副題を足せます
- 撮ったら、下端が切れていないかを目視で確認します

候補は `base-tokens.css` を土台に描かれるので、`tokens.css` を更新した後でも、そのラウンドの見た目で撮れます。

ラウンドの候補とは別の比較を ADR に添えたいときは、`--spec` で候補定義のファイルを差し替えます（既定は `<round>/candidates.mjs`）。例えば [ADR-0011](./adr/0011-switch-off-track.md) の画像は、スイッチ OFF の輪郭の有無だけを並べる定義 `design/rounds/r02/adr-0011-switch.mjs` で撮りました。

```sh
node design/tools/compare.mjs design/rounds/r02 \
  --spec design/rounds/r02/adr-0011-switch.mjs \
  --parts switches --pick 現行版 \
  --title "ADR-0011 スイッチ OFF のトラック" \
  --out design/adr/assets/0011-switch-off-track.png
```

### Storybook のストーリーを撮る（後半）

後半の ADR に添える比較画像は、比較のストーリーをそのまま撮ります。

```sh
node design/tools/capture-story.mjs design-review-01-focus--candidates \
  --pick A \
  --out design/adr/assets/NNNN-kebab-title.png
```

- Storybook をビルドし、ローカルで配信してストーリーだけを撮ります。ビルド済みのディレクトリがあれば、`--static-dir` で渡すとビルドを省きます
- `--pick` で、採用した案に「採用」の印を付けます（現行版は `current`）
- `--density coarse|fine` で密度を固定します。`--width`・`--height` で画像の大きさを調整します（既定は 1320×1500px）
- 動きを止めて撮ります（`prefers-reduced-motion`）。移り変わりの途中で撮られ、状態の見た目が出ないことがあるためです
- マウスで操作している環境（`hover: hover`、`pointer: fine`）として撮ります。撮影に使う headless Chrome は既定で「マウスなし」になり、hover の見た目が出ないためです。`--density coarse` のときは、指で操作している環境（`hover: none`、`pointer: coarse`）として撮ります
- 撮ったら、下端が切れていないかを目視で確認します

### 和文の縦位置を測る

入力欄の中で、文字のインクが上下どちらに寄っているかを画素で測ります（[`principles.md` の「和文の縦位置」](./principles.md#和文の縦位置)）。フォントの縦位置を補正したら、このコマンドで確かめます。

```sh
node design/tools/measure-text-offset.mjs
```

## ラウンド後に更新するもの

次の順で更新します。

1. **ADR を書く**: 決まったこと1つにつき1本。前の決定を覆した場合は、古い ADR を Superseded にします。全却下のラウンドも1本残します
2. **`tokens.css` を更新する**: 採用した候補の値を現行版に反映します
3. **`principles.md` の印を更新する**: 決まった軸は【前半の軸】【後半の軸】から【決定】に変え、根拠の ADR へリンクします。反例が増えた場合は「反例」の節に足します
4. **比較画像を撮る**: `tools/compare.mjs` で撮り、`adr/assets/NNNN-*.png` に保存します
